# Experiments

Scripts for longer runs. They are not the unit-test suite.

- `benchmark_toy.py` — recover known label distributions from hard labels.
- `run_annotation_benchmark.py` — hold-out or cross-dataset comparison of scLDL vs logistic/SVM/kNN/scanpy ingest (and optional CellTypist/scANVI).
